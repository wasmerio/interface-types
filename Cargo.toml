[workspace]
members = [
    "crates/to-bytes",
    "crates/it-types",
    "wasmer-it",
]

[profile.release]
opt-level = 3
debug = false
lto = true
debug-assertions = false
overflow-checks = false
panic = "abort"
